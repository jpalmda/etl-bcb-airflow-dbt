name: 'pipeline_bcb'
version: '1.0.0'
config-version: 2

profile: 'pipeline_bcb'

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  pipeline_bcb:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts